node_modules/
.next/
*.db
*.db-journal
.env
.env.local
.env.production.local
.vercel
.claude/settings.local.json
data/research-queue/
tsconfig.tsbuildinfo
.tmp-hymnal-pull/
.tmp-renders/

# Master NXR Hymnal PDF (built locally via scripts/build_nxr_hymnal_pdf.py)
# Too large for git; host externally if shipping in production.
public/hymnal-media/nxr-hymnal.pdf

__pycache__/
*.bak-*
episc_dump.txt
.tools/

# Hymnal media now lives in Cloudflare R2 (see next.config.ts redirect).
# ~8.8 GB / 13,678 files - kept on disk, never committed.
public/hymnal-media/

# Outreach target lists are strategy, not directory data — keep them local.
data/AAM_*_outreach_*.csv

# scratch query files
prisma/_q.ts
